export const DEFAULT_APP_PAGINATION = 1;
export const DEFAULT_APP_LIMIT = 50;
export const SALT_ROUNDS = 10;
export const COOKIE_EXPIRATION = new Date(Date.now() + 60 * 60 * 1000);
export const COOKIE_EXPIRATION_NEW = new Date(Date.now() + 60 * 1000); // One minute in milliseconds
